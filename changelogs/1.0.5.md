# 1.0.5

### Bug Fixes
- **Fixed TARDIS Animation Timing**: Synchronized dematerialization and rematerialization animations with their sound effects. Demat now takes ~20 seconds (matching `demat_sound.ogg`) and remat takes ~24 seconds (matching `remat_sound.ogg`).
