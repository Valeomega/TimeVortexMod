# 1.0.4

### Bug Fixes
- **Fixed KeypadScreen Crash**: Resolved a `StackOverflowError` that occurred when selecting saved locations in the Destination Configurator. The crash was caused by an infinite recursion loop between the location list and the name filter field.
